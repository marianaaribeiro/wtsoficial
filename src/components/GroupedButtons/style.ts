import { CSSProperties } from "react";

const useStyle = () => {
    const buttonStyleDefault: CSSProperties = {
        margin: '5px'
    };

    return {
        buttonStyleDefault
    }
};

export default useStyle;
